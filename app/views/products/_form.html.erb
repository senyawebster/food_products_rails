<%= form_for @product do |p| %>
  <%= p.label :name %>
  <%= p.text_field :name %>

  <%= p.label :cost %>
  <%= p.number_field(:cost, :price, in: 1.0..100.0, step: 0.01) %>


  <%= p.label :country_of_origin %>
  <%= p.text_field :country_of_origin %>

  <%= p.submit %>
<% end %>
