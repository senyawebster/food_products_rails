<%= form_for @product do |p| %>
  <%= p.label :name %>
  <%= p.text_field :name %>

  <%= p.label :cost %>
  <%= p.number_field(:cost, in: 1.0..100.0) %>


  <%= p.label :country_of_origin %>
  <%= p.text_field :country_of_origin %>

  <%= p.submit %>
<% end %>
